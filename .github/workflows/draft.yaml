name: show draft posts

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      node-version: [12.x]

  steps:
    uses: peter-evans/create-or-update-comment@v1.4.4
    with:
      issue-number: 1
      body: |
        hello world
